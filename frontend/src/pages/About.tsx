export const About = () => {
    return (
        <>
            <h1>About</h1>

            <h2>This website represents a repository of quotations of famous people.
                Users can browse random quotes, or random quotes in specified language(Ukrainian or English).
                Individual quotes contain a unique link, and can be bookmarked. To post a new quote user must be
                authenticated.
            </h2>

            <h2>There are two ways to publish a new quote: user can fill a form on the profile page,
                or upload an XML file. The template, which available in a user profile, contains an XSD schema to ease
                editing.
            </h2>
        </>
    );
}